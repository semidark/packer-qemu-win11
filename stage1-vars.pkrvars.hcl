# Stage 1 specific variables

# Output directory for stage 1 artifacts
output_directory = "output-stage1"

# VM name for stage 1
vm_name = "stage1-base"

# Stage 1 specific overrides (if any)
# Currently no specific overrides needed for stage 1